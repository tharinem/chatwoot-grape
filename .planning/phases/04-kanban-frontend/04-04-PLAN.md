---
phase: 04-kanban-frontend
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - kanban-frontend/src/components/board/KanbanBoard.vue
  - kanban-frontend/src/components/board/KanbanColumn.vue
  - kanban-frontend/src/components/board/ColumnHeader.vue
  - kanban-frontend/src/components/board/AddStageButton.vue
  - kanban-frontend/src/components/layout/BoardTopBar.vue
  - kanban-frontend/src/components/layout/FilterBar.vue
  - kanban-frontend/src/components/detail/CardSlidePanel.vue
  - kanban-frontend/src/components/detail/CardForm.vue
  - kanban-frontend/src/components/stage/StageColorPicker.vue
  - kanban-frontend/src/components/shared/ConfirmDialog.vue
  - kanban-frontend/src/components/shared/DropdownMenu.vue
  - kanban-frontend/src/components/shared/FieldVisibilityMenu.vue
  - kanban-frontend/src/composables/useFilters.ts
  - kanban-frontend/src/views/BoardView.vue
autonomous: false
requirements: [KANB-01, KANB-02, KANB-03, KANB-04, KANB-05, KANB-06, KANB-07, KANB-08]

must_haves:
  truths:
    - "Admin can create a new stage by clicking '+' at the end of the board"
    - "Admin can rename a stage by clicking on the column title"
    - "Admin can delete a stage, and if it has cards, is prompted to choose a destination stage first"
    - "Admin can change a stage's accent color via color picker"
    - "User can filter the board by agent, channel, date, or 'Atribuidos a mim'"
    - "User can create a card manually via '+' on column header or 'Novo card' in top bar"
    - "User can click a card to see full details in a slide-in panel from the right"
    - "User can toggle which fields are visible on cards via the field visibility menu"
  artifacts:
    - path: "kanban-frontend/src/components/board/ColumnHeader.vue"
      provides: "Inline editable stage header with menu trigger and add card button"
      contains: "rename"
    - path: "kanban-frontend/src/components/board/AddStageButton.vue"
      provides: "'+' column at end of board for new stage creation"
      contains: "createStage"
    - path: "kanban-frontend/src/components/layout/FilterBar.vue"
      provides: "Multi-criteria filter bar with quick 'Atribuidos a mim' button"
      contains: "assignedToMe"
    - path: "kanban-frontend/src/components/detail/CardSlidePanel.vue"
      provides: "Right slide-in panel for card details"
      contains: "slidePanel"
    - path: "kanban-frontend/src/components/detail/CardForm.vue"
      provides: "Manual card creation form"
      contains: "contact_name"
    - path: "kanban-frontend/src/components/shared/ConfirmDialog.vue"
      provides: "Destructive action confirmation modal"
      contains: "confirm"
    - path: "kanban-frontend/src/components/shared/FieldVisibilityMenu.vue"
      provides: "Toggle switches for card field visibility"
      contains: "visibleFields"
  key_links:
    - from: "kanban-frontend/src/components/board/ColumnHeader.vue"
      to: "kanban-frontend/src/stores/board.ts"
      via: "calls updateStage for rename, deleteStage for delete"
      pattern: "updateStage|deleteStage"
    - from: "kanban-frontend/src/components/detail/CardForm.vue"
      to: "kanban-frontend/src/stores/board.ts"
      via: "calls createCard on form submit"
      pattern: "createCard"
    - from: "kanban-frontend/src/components/layout/FilterBar.vue"
      to: "kanban-frontend/src/composables/useFilters.ts"
      via: "applies filter criteria to cards"
      pattern: "useFilters"
---

<objective>
Complete all remaining Kanban features: inline stage management (create, rename, reorder, delete with card migration), filter bar with multi-criteria filtering and "Atribuidos a mim" quick filter, manual card creation form, card detail slide panel, stage color picker, field visibility menu, confirm dialogs, and dropdown menus. This finishes all KANB requirements.

Purpose: Deliver the full feature set for Phase 4 -- admins manage the pipeline structure, users filter and create cards, card details are viewable in a slide panel.
Output: Complete Kanban board SPA with all KANB-01 through KANB-08 requirements fulfilled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-kanban-frontend/04-CONTEXT.md
@.planning/phases/04-kanban-frontend/04-RESEARCH.md
@.planning/phases/04-kanban-frontend/04-UI-SPEC.md
@.planning/phases/04-kanban-frontend/04-01-SUMMARY.md
@.planning/phases/04-kanban-frontend/04-02-SUMMARY.md
@.planning/phases/04-kanban-frontend/04-03-SUMMARY.md

<interfaces>
<!-- Components and stores from Plans 01-03 that this plan extends/consumes -->

From kanban-frontend/src/stores/board.ts:
```typescript
// Actions this plan will call:
createStage(input: CreateStageInput): Promise<void>
updateStage(id: string, input: UpdateStageInput): Promise<void>
deleteStage(id: string): Promise<void>
reorderStages(items: ReorderStageItem[]): Promise<void>
createCard(stageId: string, input: CreateCardInput): Promise<void>
updateCard(id: string, input: UpdateCardInput): Promise<void>
deleteCard(id: string): Promise<void>
```

From kanban-frontend/src/stores/ui.ts:
```typescript
// State and actions this plan will use:
visibleFields: string[]  // default: ['channelType', 'createdAt']
slidePanel: { isOpen: boolean, cardId: string | null }
activeFilters: { agentIds: number[], channelTypes: string[], datePreset: string | null, assignedToMe: boolean }
toggleField(field: string): void
openSlidePanel(cardId: string): void
closeSlidePanel(): void
setFilter(key: string, value: unknown): void
clearFilters(): void
toggleAssignedToMe(): void
```

From kanban-frontend/src/stores/auth.ts:
```typescript
// For "Atribuidos a mim" filter:
userId: number | null
accountId: number | null
```

From kanban-frontend/src/types/card.ts:
```typescript
interface CreateCardInput {
  contact_name: string;
  conversation_id?: number;
  channel_type?: string;
  assignee_id?: number;
  custom_fields?: Record<string, unknown>;
}
```

From kanban-frontend/src/types/stage.ts:
```typescript
interface CreateStageInput { name: string; color?: string; }
interface UpdateStageInput { name?: string; color?: string | null; }
interface ReorderStageItem { id: string; position: number; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build stage management components (ColumnHeader, AddStageButton, StageColorPicker), shared utility components (ConfirmDialog, DropdownMenu, FieldVisibilityMenu), and FilterBar with useFilters composable</name>
  <files>
    kanban-frontend/src/components/board/ColumnHeader.vue,
    kanban-frontend/src/components/board/AddStageButton.vue,
    kanban-frontend/src/components/stage/StageColorPicker.vue,
    kanban-frontend/src/components/shared/ConfirmDialog.vue,
    kanban-frontend/src/components/shared/DropdownMenu.vue,
    kanban-frontend/src/components/shared/FieldVisibilityMenu.vue,
    kanban-frontend/src/components/layout/FilterBar.vue,
    kanban-frontend/src/composables/useFilters.ts
  </files>
  <read_first>
    kanban-frontend/src/stores/board.ts,
    kanban-frontend/src/stores/ui.ts,
    kanban-frontend/src/stores/auth.ts,
    kanban-frontend/src/types/stage.ts,
    kanban-frontend/src/types/card.ts,
    kanban-frontend/src/i18n/pt-BR.json,
    kanban-frontend/src/components/board/KanbanColumn.vue,
    kanban-frontend/src/components/layout/BoardTopBar.vue,
    .planning/phases/04-kanban-frontend/04-UI-SPEC.md,
    .planning/phases/04-kanban-frontend/04-CONTEXT.md
  </read_first>
  <action>
**ColumnHeader.vue (per D-07 inline stage editing):**
- Props: `stage: Stage`, `cardCount: number`, `isAdmin: boolean`
- Emits: `addCard`, `deleteStage`
- **Color accent bar:** `<div class="h-1 rounded-t-xl" :style="{ backgroundColor: stage.color || '#7B5EA7' }" />`
- **Stage name (inline editable per D-07):**
  - Default state: `<h3 class="text-base font-semibold text-n-slate-12 cursor-pointer hover:bg-n-alpha-2 rounded px-1 -mx-1 transition-colors" @click="startEditing">{{ stage.name }}</h3>`
  - Edit state: `<input v-model="editName" class="text-base font-semibold text-n-slate-12 bg-n-slate-1 border border-n-strong rounded px-1 -mx-1 w-full outline-none" @keydown.enter="saveRename" @keydown.escape="cancelRename" @blur="saveRename" />`
  - On save: call `boardStore.updateStage(stage.id, { name: editName })`, show `showToast('success', ...)`, revert to display mode
  - On cancel: revert to display mode, restore original name
- **Card count:** `<span class="text-xs text-n-slate-11 ml-1">({{ cardCount }})</span>` beside the name
- **Right side buttons (only if isAdmin):**
  - Add card button: `<button @click="$emit('addCard')" class="w-8 h-8 flex items-center justify-center rounded hover:bg-n-alpha-2 text-n-slate-11 hover:text-n-slate-12 transition-colors" :aria-label="t('aria.addCardToStage')" :title="t('tooltip.addCard')"><span class="i-lucide-plus w-4 h-4" /></button>`
  - Menu trigger: `<button @click="showMenu = !showMenu" class="w-8 h-8 flex items-center justify-center rounded hover:bg-n-alpha-2 text-n-slate-11 hover:text-n-slate-12 transition-colors" :aria-label="t('aria.stageMenu', { stageName: stage.name })" :title="t('tooltip.stageOptions')"><span class="i-lucide-ellipsis w-4 h-4" /></button>`
- **Dropdown menu items:** Rename, Change Color (opens StageColorPicker), Delete Stage
  - Delete: if `cardCount > 0`, emit `deleteStage` event (parent shows ConfirmDialog with stage picker per D-08)
  - Delete: if `cardCount === 0`, emit `deleteStage` event (parent shows simple confirm per D-08)
- Use `onClickOutside` from `@vueuse/core` to close the dropdown menu

**AddStageButton.vue (per D-07):**
- Renders at the end of the board columns: `<div class="w-[280px] flex-shrink-0">`
- Default state: `<button class="w-full h-12 border-2 border-dashed border-n-weak rounded-xl flex items-center justify-center gap-2 text-n-slate-11 hover:text-n-slate-12 hover:border-n-strong transition-colors" @click="startAdding" :aria-label="t('aria.addNewStage')" :title="t('tooltip.newStage')"><span class="i-lucide-plus w-5 h-5" />{{ t('tooltip.newStage') }}</button>`
- Adding state: Show input with placeholder from `t('stage.namePlaceholder')` ("Nome do estagio"), auto-focus on render, Enter calls `boardStore.createStage({ name: inputValue })` then `showToast('success', t('toast.stageCreated'))`, Escape cancels

**StageColorPicker.vue (per D-12):**
- Props: `currentColor: string | null`, `stageId: string`
- Popover with preset color swatches:
  - Colors: `['#7B5EA7', '#3B82F6', '#EF4444', '#F59E0B', '#10B981', '#EC4899', '#8B5CF6', '#06B6D4', '#6B7280', '#D97706']`
  - Each swatch: `<button class="w-8 h-8 rounded-full border-2 transition-transform hover:scale-110" :class="color === currentColor ? 'border-n-slate-12 scale-110' : 'border-transparent'" :style="{ backgroundColor: color }" @click="selectColor(color)" />`
  - "Remove color" option: resets to null
  - On select: call `boardStore.updateStage(stageId, { color: selectedColor })`
- Use `onClickOutside` to close

**ConfirmDialog.vue (per D-08):**
- Props: `isOpen: boolean`, `title: string`, `message: string`, `confirmLabel: string`, `variant: 'danger' | 'default'`
- Optional slot for extra content (stage selector for delete-with-cards)
- Emits: `confirm`, `cancel`
- Modal overlay: `fixed inset-0 z-50 flex items-center justify-center bg-black/50`
- Dialog card: `bg-n-slate-1 rounded-xl shadow-2xl max-w-md w-full mx-4 p-6`
- Title: `text-base font-semibold text-n-slate-12`
- Message: `text-sm text-n-slate-11`
- Buttons:
  - Cancel: `px-4 py-2 text-sm text-n-slate-11 hover:bg-n-alpha-2 rounded-lg transition-colors` with text from `t('card.form.cancel')` ("Descartar card") or a generic "Cancelar"
  - Confirm (danger): `px-4 py-2 text-sm bg-n-ruby-9 text-white rounded-lg hover:bg-n-ruby-10 transition-colors` with confirmLabel
  - Confirm (default): `px-4 py-2 text-sm bg-n-brand text-white rounded-lg hover:opacity-90 transition-opacity` with confirmLabel
- Close on Escape key, close on overlay click
- **Stage delete with cards (D-08):** When deleting a stage that has cards, the parent (KanbanColumn or BoardView) opens ConfirmDialog with:
  - Title: "Excluir estagio"
  - Message: `t('stage.deleteConfirmWithCards', { count: cardCount })`
  - A `<select>` in the default slot showing all OTHER stages as destination options
  - On confirm: the parent batch-PATCHes all cards to destination stage (using `Promise.all(cards.map(c => boardStore.updateCard(c.id, { stage_id: destinationStageId })))`) THEN calls `boardStore.deleteStage(stageId)`, then `showToast('success', t('toast.stageDeleted'))`

**DropdownMenu.vue:**
- Props: `items: Array<{ label: string, icon?: string, variant?: 'danger' | 'default', action: () => void }>`
- Container: `bg-n-slate-1 border border-n-weak rounded-lg shadow-lg py-1 min-w-[160px] z-50`
- Each item: `<button class="w-full text-left px-3 py-2 text-sm flex items-center gap-2 hover:bg-n-alpha-2 transition-colors" :class="item.variant === 'danger' ? 'text-n-ruby-11' : 'text-n-slate-12'" @click="item.action()"><span v-if="item.icon" :class="[item.icon, 'w-4 h-4']" />{{ item.label }}</button>`

**FieldVisibilityMenu.vue (per D-02):**
- Uses `useUiStore()` to read/toggle `visibleFields`
- Popover content: list of toggleable fields:
  - `channelType` -- labeled `t('card.form.channel')` ("Canal de origem")
  - `assigneeId` -- labeled `t('card.form.agent')` ("Agente responsavel")
  - `createdAt` -- labeled as "Data de entrada"
  - `conversationId` -- labeled `t('card.form.chatwootLink')` ("Link da conversa")
- Each field: `<label class="flex items-center justify-between px-3 py-2 hover:bg-n-alpha-2 cursor-pointer"><span class="text-sm text-n-slate-12">{{ label }}</span><input type="checkbox" :checked="uiStore.visibleFields.includes(field)" @change="uiStore.toggleField(field)" class="accent-n-brand" /></label>`
- Container: `bg-n-slate-1 border border-n-weak rounded-lg shadow-lg py-1 min-w-[200px]`
- Trigger button in BoardTopBar: `<button class="w-8 h-8 flex items-center justify-center rounded hover:bg-n-alpha-2 text-n-slate-11" :aria-label="t('aria.fieldVisibility')" :title="t('tooltip.fieldVisibility')"><span class="i-lucide-sliders-horizontal w-4 h-4" /></button>`

**FilterBar.vue (per D-09):**
- Uses `useFilters` composable and `useUiStore`
- Layout: `<div class="flex items-center gap-2 flex-wrap">`
- **"Atribuidos a mim" button (per D-09):**
  - `<button class="px-3 py-1.5 text-xs font-semibold rounded-full border transition-colors" :class="uiStore.activeFilters.assignedToMe ? 'bg-n-brand/10 text-n-blue-11 border-n-blue-7' : 'text-n-slate-11 border-n-weak hover:border-n-strong'" @click="uiStore.toggleAssignedToMe()">{{ t('board.assignedToMe') }}</button>`
- **Agent filter:** `<select>` dropdown populated from unique `assigneeId` values across all cards (from `boardStore.allCards`). Multi-select simulation with chips/badges.
- **Channel filter:** `<select>` dropdown from unique `channelType` values.
- **Date range filter:** Preset options: "Hoje", "Ultimos 7 dias", "Ultimos 30 dias", "Todos". Simple `<select>`.
- **Clear filters:** `<button v-if="hasActiveFilters" class="text-xs text-n-blue-11 hover:text-n-blue-12" @click="uiStore.clearFilters()">{{ t('filter.clearAll') }}</button>`

**useFilters.ts composable (per D-09, KANB-06):**
- Import `useBoardStore`, `useUiStore`, `useAuthStore`
- Computed `filteredCardsByStage`: takes `boardStore.cardsByStage` and filters each stage's cards array based on `uiStore.activeFilters`:
  - `assignedToMe`: filter cards where `card.assigneeId === authStore.userId`
  - `agentIds`: filter cards where `card.assigneeId` is in the selected agent IDs array
  - `channelTypes`: filter cards where `card.channelType` is in the selected channels array
  - `datePreset`: filter cards where `card.createdAt` is within the date range:
    - "today": same day as now
    - "7days": within last 7 days (use `date-fns` `isAfter(parseISO(card.createdAt), subDays(new Date(), 7))`)
    - "30days": within last 30 days
    - "all" or null: no filter
- Export `filteredCardsByStage` computed and `hasActiveFilters` computed (any filter active)
  </action>
  <verify>
    <automated>cd kanban-frontend && npx vue-tsc --noEmit && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>All stage management UI (create, rename, delete with card migration, color picker), shared utility components (confirm dialog, dropdown menu, field visibility toggles), and filter bar with multi-criteria filtering are complete.</done>
</task>

<task type="auto">
  <name>Task 2: Build CardSlidePanel, CardForm, wire ColumnHeader into KanbanColumn, wire FilterBar and FieldVisibilityMenu into BoardTopBar, integrate useFilters into BoardView</name>
  <files>
    kanban-frontend/src/components/detail/CardSlidePanel.vue,
    kanban-frontend/src/components/detail/CardForm.vue,
    kanban-frontend/src/components/board/KanbanBoard.vue,
    kanban-frontend/src/components/board/KanbanColumn.vue,
    kanban-frontend/src/components/layout/BoardTopBar.vue,
    kanban-frontend/src/views/BoardView.vue
  </files>
  <read_first>
    kanban-frontend/src/stores/board.ts,
    kanban-frontend/src/stores/ui.ts,
    kanban-frontend/src/stores/auth.ts,
    kanban-frontend/src/types/card.ts,
    kanban-frontend/src/types/stage.ts,
    kanban-frontend/src/i18n/pt-BR.json,
    kanban-frontend/src/composables/useFilters.ts,
    kanban-frontend/src/composables/useToast.ts,
    kanban-frontend/src/components/board/KanbanBoard.vue,
    kanban-frontend/src/components/board/KanbanColumn.vue,
    kanban-frontend/src/components/board/ColumnHeader.vue,
    kanban-frontend/src/components/layout/BoardTopBar.vue,
    kanban-frontend/src/components/layout/FilterBar.vue,
    kanban-frontend/src/components/shared/FieldVisibilityMenu.vue,
    kanban-frontend/src/components/shared/ConfirmDialog.vue,
    kanban-frontend/src/views/BoardView.vue,
    .planning/phases/04-kanban-frontend/04-UI-SPEC.md,
    .planning/phases/04-kanban-frontend/04-CONTEXT.md
  </read_first>
  <action>
**CardSlidePanel.vue (per D-03):**
- Uses `useUiStore()` for `slidePanel.isOpen` and `slidePanel.cardId`
- Uses `useBoardStore()` to find the card by ID from `allCards`
- **Slide-in panel:**
  - Container: `<Transition name="slide">` wrapping a `<div v-if="uiStore.slidePanel.isOpen" class="fixed top-0 right-0 h-full w-[400px] bg-n-slate-1 border-l border-n-weak shadow-2xl z-40 flex flex-col">`
  - Transition classes: `slide-enter-active: transition-transform duration-250 ease-out`, `slide-enter-from: translate-x-full`, `slide-leave-active: transition-transform duration-200 ease-in`, `slide-leave-to: translate-x-full`
  - **NOTE:** Since Tailwind-only is required, define these transitions using Tailwind arbitrary variants or inline the Transition component's CSS classes as standard Vue transition classes (this is acceptable as they are Vue framework transitions, not custom styling).
- **Header:**
  - Close button: `<button @click="uiStore.closeSlidePanel()" class="w-8 h-8 flex items-center justify-center rounded hover:bg-n-alpha-2 text-n-slate-11" :aria-label="t('aria.closePanel')" :title="t('tooltip.close')"><span class="i-lucide-x w-5 h-5" /></button>`
  - Expand button: `<button class="w-8 h-8 flex items-center justify-center rounded hover:bg-n-alpha-2 text-n-slate-11" :aria-label="t('aria.expandPanel')" :title="t('tooltip.expand')"><span class="i-lucide-maximize-2 w-5 h-5" /></button>` (for v1, expand toggles panel width to `w-[640px]` and centers it as an overlay)
- **Content (all card fields displayed):**
  - Contact name: `<h2 class="text-xl font-semibold text-n-slate-12">{{ card.contactName }}</h2>`
  - Stage: show current stage name with colored dot
  - Channel: `text-sm text-n-slate-11` with icon
  - Agent: `text-sm text-n-slate-11` with `i-lucide-user` icon
  - Created at: formatted with `date-fns format(parseISO(card.createdAt), "dd MMM yyyy 'as' HH:mm", { locale: ptBR })`
  - Chatwoot link (per KANB-05): `<a :href="chatwootUrl" target="_blank" rel="noopener" class="inline-flex items-center gap-2 text-sm text-n-blue-11 hover:text-n-blue-12"><span class="i-lucide-external-link w-4 h-4" />{{ t('slidePanel.openInChatwoot') }}</a>`. URL: `${import.meta.env.VITE_CHATWOOT_URL}/app/accounts/${card.accountId}/conversations/${card.conversationId}`. Only show if `card.conversationId` is not null.
  - Custom fields: render as key-value pairs if present
- **Delete card button:** At bottom of panel, `<button class="text-sm text-n-ruby-11 hover:text-n-ruby-12 flex items-center gap-1" @click="confirmDeleteCard"><span class="i-lucide-trash-2 w-4 h-4" />{{ t('stage.menu.delete') }}</button>`. On click, show ConfirmDialog with `t('card.deleteConfirm', { contactName: card.contactName })`, on confirm call `boardStore.deleteCard(card.id)` then `uiStore.closeSlidePanel()`
- Close on Escape key: use `onKeyStroke('Escape', () => uiStore.closeSlidePanel())` from `@vueuse/core`
- Close on click outside panel (optional -- clicking the overlay area)

**CardForm.vue (per D-08, D-11, KANB-08):**
- Props: `stageId: string | null` (pre-selected stage, null if opened from global CTA), `isOpen: boolean`
- Emits: `close`
- Form renders in a modal or slide panel overlay
- **Fields:**
  - Contact name (required): `<input v-model="form.contact_name" class="w-full px-3 py-2 text-sm bg-n-slate-2 border border-n-weak rounded-lg text-n-slate-12 placeholder:text-n-slate-9 focus:border-n-strong focus:outline-none" :placeholder="t('card.form.contactNamePlaceholder')" />`
  - Stage selector: `<select v-model="form.stage_id" class="w-full px-3 py-2 text-sm bg-n-slate-2 border border-n-weak rounded-lg text-n-slate-12">` populated with `boardStore.sortedStages`. Pre-select `stageId` prop if provided.
  - Channel (optional): `<input v-model="form.channel_type" ... />` (text input)
  - Agent ID (optional): `<input v-model.number="form.assignee_id" type="number" ... />` (number input)
  - Chatwoot conversation ID (optional): `<input v-model.number="form.conversation_id" type="number" ... />`
- **Labels:** All via `t()` -- `t('card.form.contactName')`, `t('card.form.stage')`, `t('card.form.channel')`, `t('card.form.agent')`, `t('card.form.chatwootLink')`
- **Submit button:** `<button class="px-4 py-2 bg-n-brand text-white text-sm font-semibold rounded-lg hover:opacity-90 transition-opacity disabled:opacity-50" :disabled="!form.contact_name.trim()" @click="submitCard">{{ t('card.form.submit') }}</button>` ("Criar card")
- **Cancel button:** `<button class="px-4 py-2 text-sm text-n-slate-11 hover:bg-n-alpha-2 rounded-lg transition-colors" @click="$emit('close')">{{ t('card.form.cancel') }}</button>` ("Descartar card")
- **On submit:** Build `CreateCardInput` from form state (snake_case keys per API convention), call `boardStore.createCard(selectedStageId, input)`, `showToast('success', t('toast.cardCreated'))`, emit `close`

**Wire ColumnHeader into KanbanColumn.vue:**
- Replace the existing inline header in KanbanColumn with `<ColumnHeader>` component
- Pass props: `:stage="stage"`, `:card-count="cards.length"`, `:is-admin="authStore.role === 'administrator'"` (or check role from auth store)
- Handle `@addCard` event: open CardForm with `stageId` pre-selected
- Handle `@deleteStage` event: if `cards.length > 0`, show ConfirmDialog with stage selector (D-08 flow); if empty, show simple ConfirmDialog. After confirm, execute the batch-move + delete pattern.

**Wire FilterBar and FieldVisibilityMenu into BoardTopBar.vue:**
- Add `FilterBar` component below the title row
- Add `FieldVisibilityMenu` trigger button in the header right side (next to "Novo card" button)

**Integrate useFilters into BoardView.vue:**
- Import `useFilters` composable
- Replace direct `boardStore.cardsByStage` usage with `filteredCardsByStage` from `useFilters`
- Pass `filteredCardsByStage` to `KanbanBoard` (or KanbanBoard reads from composable)
- Handle "Novo card" button click from BoardTopBar: open CardForm with `stageId: null` (user picks stage)
- Handle EmptyBoard CTA click: same as "Novo card"
- Include `CardSlidePanel` component in BoardView template

**Wire AddStageButton into KanbanBoard.vue:**
- Replace the placeholder div at the end of columns with actual `AddStageButton` component
  </action>
  <verify>
    <automated>cd kanban-frontend && npx vue-tsc --noEmit && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>Complete Kanban frontend SPA: CardSlidePanel shows full card details, CardForm enables manual card creation from column header or global CTA, ColumnHeader has inline rename/delete/color/add card, FilterBar filters by agent/channel/date with "Atribuidos a mim", FieldVisibilityMenu toggles card fields, all components wired into BoardView. All KANB-01 through KANB-08 requirements are addressed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Kanban board</name>
  <files>kanban-frontend/src/views/BoardView.vue</files>
  <action>
Human verifies the complete Kanban Frontend SPA end-to-end. All automation (Tasks 1-2) must be complete before this checkpoint.

Verification steps:
1. Start the Kanban API: cd kanban-api and npm run dev (needs PostgreSQL running)
2. Start the frontend: cd kanban-frontend and npx vite
3. Open http://localhost:5173 in browser -- should redirect to login
4. Enter your Chatwoot user_access_token and account_id, click login
5. Verify board loads with pipeline stages as columns
6. Drag a card from one column to another -- should move smoothly, toast confirms
7. Click a card -- slide panel opens from right showing full details
8. Click "Novo card" in top bar -- card creation form opens
9. Fill contact name, select stage, submit -- card appears in correct column
10. Click "+" on a column header -- card form opens with that stage pre-selected
11. Click stage title -- should become editable inline, type new name, press Enter
12. Click "..." on column header -- dropdown with Rename, Color, Delete options
13. Try "Atribuidos a mim" filter button
14. Try field visibility toggles (sliders icon in top bar)
15. Verify empty state shows if all cards are removed
16. Verify all text is in Portuguese
  </action>
  <verify>Human confirms all 16 verification steps pass. Type "approved" or describe issues to fix.</verify>
  <done>Human has verified all KANB-01 through KANB-08 requirements work correctly in the browser.</done>
</task>

</tasks>

<verification>
1. `cd kanban-frontend && npx vue-tsc --noEmit` exits 0
2. `cd kanban-frontend && npx vite build` exits 0
3. All KANB requirements covered:
   - KANB-01: Board renders columns per stage (KanbanBoard + KanbanColumn)
   - KANB-02: Drag-and-drop between columns (vue-draggable-plus with group: 'cards')
   - KANB-03: Admin stage CRUD (ColumnHeader, AddStageButton, ConfirmDialog with stage migration)
   - KANB-04: Card shows contact name, channel, date, agent (KanbanCard with visibleFields)
   - KANB-05: Chatwoot conversation link (KanbanCard + CardSlidePanel)
   - KANB-06: Filter by agent (FilterBar + useFilters with "Atribuidos a mim")
   - KANB-07: Empty state (EmptyBoard with CTA)
   - KANB-08: Manual card creation (CardForm with two entry points)
4. No hardcoded strings -- all copy via vue-i18n pt-BR
5. No scoped CSS or inline styles (except stage color bar)
6. All icon-only buttons have aria-label attributes
</verification>

<success_criteria>
- Admin can create, rename, reorder, delete stages directly from the board
- Deleting a stage with cards prompts for destination stage selection
- Stage accent colors are configurable via color picker
- User can filter board by agent, channel, date range, or "Atribuidos a mim"
- User can create cards manually from column header "+" or global "Novo card"
- Card click opens slide panel with full details including Chatwoot link
- Field visibility menu lets user toggle which fields appear on cards
- Human verification confirms all interactions work end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-kanban-frontend/04-04-SUMMARY.md`
</output>
